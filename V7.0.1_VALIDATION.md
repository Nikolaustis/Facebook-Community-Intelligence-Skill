# V7.0.1 Validation

## Executed checks

- `node --check scripts/phase1_collect_candidates.js` passed.
- `node tests/test_phase1_recovery_static.js` passed.
- `node tests/test_phase15_name_prefilter.js` passed.
- All JavaScript files passed `node --check`.
- All JSON files parsed successfully.
- The Phase 2 authoritative XLSX field order was not changed.
- The package excludes `runs/`, `config/`, `node_modules/`, `tmp/`, and `work/`.

## Expected runtime evidence

Phase 1 stats now expose `raw_group_link_count`, the selected search URL, route fallback use, login/checkpoint signals, temporary-error signals, and no-result signals. A final zero creates files under `phase1_diagnostics/`.

Interpretation:

- `raw_group_link_count > 0` with zero candidates would indicate an extractor regression.
- `raw_group_link_count = 0` on route 1 followed by candidates on route 2 confirms route recovery.
- `LOGIN_OR_CHECKPOINT_SIGNAL` or `FACEBOOK_TEMPORARY_ERROR` is no longer mislabeled as “no groups.”
- `ZERO_CANDIDATES_AFTER_ROUTE_FALLBACK` is an explicit unresolved false-zero candidate and must be reviewed through the generated diagnostics.

## Environment limitation

The build environment cannot connect to the colleague's authenticated Facebook CDP session. Live confirmation must therefore be performed on that Windows machine. The package now preserves enough evidence to diagnose any remaining account-specific Facebook layout without another blind patch.
