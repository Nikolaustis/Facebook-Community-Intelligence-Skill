# 使用说明

1. 将本压缩包内容解压到现有仓库根目录，并覆盖同名文件。
2. 在仓库根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\APPLY_PROJECT_METADATA_CLEANUP.ps1
```

3. 脚本会：
   - 从 Phase 1 运行输出中移除项目发布版本字段；
   - 删除文件名中带项目发布标识的旧文件；
   - 保留新的稳定 monitor 入口、GeoNames 安全策略和通用 metadata test；
   - 执行完成后自动删除迁移脚本自身。
4. 然后运行：

```powershell
npm ci
npm test
npm run doctor
```

说明：此处理移除的是**项目自身的版本化信息**。`package.json` / `package-lock.json` 中第三方依赖的版本约束、npm lockfile schema 以及文档中的运行时最低版本要求仍会保留，否则会破坏依赖可复现性和兼容性声明。
