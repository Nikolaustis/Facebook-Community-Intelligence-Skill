# 覆盖包使用说明

本 ZIP 是 **Facebook-Game-Group-Monitor-Skill 的增量覆盖包**，不是完整仓库。

## 使用方式

1. 先停止正在运行的 Phase 1 / Phase 1.5 / Phase 2 / 后台任务。
2. 将本 ZIP **直接解压到现有仓库根目录**，允许覆盖同名文件。
3. 不要删除仓库中本覆盖包未包含的原文件，尤其是：
   - `scripts/phase1_collect_candidates.js`
   - `scripts/phase2_collect_details.js`
   - `scripts/start_background_task.ps1`
   - `scripts/scheduled_phase2_runner.ps1`
   - `scripts/queue_phase2_after_current.ps1`
4. 在仓库根目录运行：

```powershell
npm ci
npm test
npm run doctor
```

5. 启动专用浏览器并确认 Facebook 登录态：

```powershell
npm run login
npm run validate-login
```

6. 测试无误后，将整个仓库提交/上传 GitHub。

## 重要说明

正常采集请使用：

```powershell
npm run phase1 -- ...
npm run phase2 -- ...
npm run monitor -- ...
```

后台入口 `phase1:bg`、`phase2:bg`、`monitor:bg` 和 `phase2:handoff` 也已接入可靠性层。

不要把直接执行：

```powershell
node scripts/phase1_collect_candidates.js
node scripts/phase2_collect_details.js
```

作为正常工作流，因为直接执行会有意绕过本覆盖包的兼容补丁。

本包没有修改版本号，也没有包含 `runs/`、本地账号配置、GeoNames 凭据或浏览器登录数据。
