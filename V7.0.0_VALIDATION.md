# V7.0.0 Validation

## Static checks

- All 21 JavaScript files under `scripts/` and `tests/` passed `node --check`.
- All six packaged JSON files parsed successfully.
- `package.json` and `package-lock.json` both report V7.0.0.

## Regression suite

`npm run phase15:test` covers 15 cases, including:

- canonical titles;
- standalone short aliases;
- `GAG` versus `GAG2`, `gags`, and `gagged`;
- separated alphanumeric aliases;
- singular full-title variants;
- connector elision;
- localized source queries;
- query-only-in-search-context rejection;
- seed and truncated-name preservation;
- explicit multi-game names.

Expected result:

```json
{"ok": true, "cases": 15}
```

## End-to-end fixture

A two-game mock index with six candidates produced:

- input: 6;
- kept: 4;
- rejected: 2;
- reduction rate: 33.3333%;
- Grow a Garden `GAG2` sibling-only candidate rejected;
- `AI gagged` rejected;
- Grow a Garden 2 `GAG2` and `Gag 2` retained;
- seed URL retained.

## Queue and cache checks

- Original `phase1_index.json` and source candidate files remained unchanged.
- First Phase 1.5 run generated the reduced queue and audit artifacts.
- A second run with unchanged inputs returned `cache_hit: true`.
- `phase15_name_prefilter.enabled=false` remained disabled when no CLI override was supplied.
- A source query identical to a sibling-owned alias was rejected rather than used to override sibling exclusion.
